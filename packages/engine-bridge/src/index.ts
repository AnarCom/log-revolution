// Wraps the wasm-bindgen output of crates/engine-wasm (browser target).
// Deferred: browser build isn't needed yet (see PLAN.md §10) — desktop
// links crates/engine natively via Tauri, no WASM involved there.
export {};
