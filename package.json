{
  "name": "revlog",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:ui": "pnpm --filter ui-shell dev",
    "build:ui": "pnpm --filter ui-shell build",
    "test": "pnpm -r test"
  }
}
